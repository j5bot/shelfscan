import { CollectionPageContent } from '@/app/ui/CollectionPageContent';

export default async function TradePage() {
    return (
        <CollectionPageContent
            title="ShelfScan | Atlas Trade"
            heading="Atlas Trade"
        />
    );
}
