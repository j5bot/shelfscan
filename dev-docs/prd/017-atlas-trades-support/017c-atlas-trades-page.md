# PRD 017C Atlas Trades Page

## Overview

Add a page `trade` to ShelfScan which sets `modeMap.trade` and is otherwise identical to the `swap` 
page.
Add a page `tradescan` to ShelfScan which sets `modeMap.tradeScan` and is otherwise identical to 
the 
`swapscan` page.

